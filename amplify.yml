version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Setting up build environment..."
        - 'echo "Node version: $(node --version)"'
        - 'echo "NPM version: $(npm --version)"'
        - echo "Clearing npm cache..."
        - npm cache clean --force
        - echo "Installing dependencies..."
        - echo "Temporarily unsetting NODE_ENV to install devDependencies..."
        - unset NODE_ENV
        - npm ci
        - echo "Verifying TypeScript installation..."
        - npm list typescript @types/react @types/node || echo "Some TypeScript packages missing"
        - echo "Validating environment variables..."
        - echo "DEBUG - JWT_SECRET is $JWT_SECRET"
        - echo "DEBUG - APP_DYNAMODB_TABLE_NAME is $APP_DYNAMODB_TABLE_NAME"
        - echo "DEBUG - APP_AWS_REGION is $APP_AWS_REGION"
        - |
          if [ -z "$JWT_SECRET" ]; then
            echo "ERROR: JWT_SECRET environment variable is required"
            exit 1
          fi
        - |
          if [ -z "$APP_AWS_REGION" ]; then
            echo "WARNING: APP_AWS_REGION not set, using default us-east-1"
            export APP_AWS_REGION=us-east-1
          fi
        - |
          if [ -z "$APP_DYNAMODB_SUBMISSIONS_GSI_CHURCH_DATE" ]; then
            echo "WARNING: APP_DYNAMODB_SUBMISSIONS_GSI_CHURCH_DATE not set, using default submissions-church-date-index"
            export APP_DYNAMODB_SUBMISSIONS_GSI_CHURCH_DATE=submissions-church-date-index
          fi
        - |
          if [ -z "$APP_ACCESS_KEY_ID" ]; then
            echo "WARNING: APP_ACCESS_KEY_ID not set, using IAM role"
          fi
        - |
          if [ -z "$APP_SECRET_ACCESS_KEY" ]; then
            echo "WARNING: APP_SECRET_ACCESS_KEY not set, using IAM role"
          fi
        - echo "Environment variables validated successfully"

        - echo "Dependencies installed successfully"
    build:
      commands:
        - echo "Building Next.js application..."
        - echo "Setting NODE_ENV=production for build"
        - export NODE_ENV=production
        - echo "Next.js will automatically run TypeScript checking during build"
        - npm run build
        - echo "Build completed successfully"
    postBuild:
      commands:
        - echo "Post-build optimizations..."
        - echo "Build artifacts ready for deployment"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
  customHeaders:
    - pattern: '**'
      headers:
        - key: 'X-Frame-Options'
          value: 'DENY'
        - key: 'X-Content-Type-Options'
          value: 'nosniff'
        - key: 'Referrer-Policy'
          value: 'strict-origin-when-cross-origin'
        - key: 'Strict-Transport-Security'
          value: 'max-age=31536000; includeSubDomains'
        - key: 'Content-Security-Policy'
          value: "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' data: https://api.arya.ai https://*.amazonaws.com; frame-ancestors 'none';"
    - pattern: '/api/**'
      headers:
        - key: 'Cache-Control'
          value: 'no-store, no-cache, must-revalidate'
        - key: 'X-Robots-Tag'
          value: 'noindex, nofollow'
    - pattern: '/admin/**'
      headers:
        - key: 'Cache-Control'
          value: 'no-store, no-cache, must-revalidate'
        - key: 'X-Robots-Tag'
          value: 'noindex, nofollow'
    - pattern: '**/*.js'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
    - pattern: '**/*.css'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
    - pattern: '**/*.{png,jpg,jpeg,gif,ico,svg,webp}'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
  environmentVariables:
    - name: JWT_SECRET
      value: q(rY0b9qTgT6ZsdpflqNJXltC6Q9PRkRX2dEj6tY8=
    - name: APP_DYNAMODB_TABLE_NAME
      value: health-screening-submissions
    - name: APP_DYNAMODB_CHURCHES_TABLE
      value: health-screening-churches
    - name: APP_DYNAMODB_USERS_TABLE
      value: health-screening-users
    - name: APP_DYNAMODB_SUBMISSIONS_GSI_CHURCH_DATE
      value: submissions-church-date-index
    - name: APP_S3_BUCKET_NAME
      value: health-screening-photos-2024
    - name: APP_SNS_ENABLED
      value: true
    - name: APP_SNS_SENDER_ID
      value: HealthCheck
    - name: APP_AWS_REGION
      value: us-east-1
    - name: NODE_ENV
      value: production
    - name: NEXT_TELEMETRY_DISABLED
      value: 1